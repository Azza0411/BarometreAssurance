# Copier en .env a la racine avant `docker compose up --build`

# MySQL (service api)
DB_NAME=MarketInsurance
DB_PASSWORD=changeme

# Chatbot IA (service chatbot) - cle Groq, jamais commitee
GROQ_API_KEY=gsk_...
